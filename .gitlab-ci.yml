# =========================================================================
# GitLab CI/CD Pipeline Configuration (Template-Only)
# =========================================================================
#
# This file includes GitLab's built-in Branch-Pipelines template.
# No custom code or bash commands are written here.
#
# Slack and Jenkins integrations are handled natively via:
# Settings > Integrations > Jenkins / Slack notifications
#
# =========================================================================

include:
  - template: Workflows/Branch-Pipelines.gitlab-ci.yml
